# Checkout-Input-Validator-for-WooCommerce
A lightweight WooCommerce plugin that prevents customers from submitting required checkout fields containing only emojis, invisible Unicode characters, zero-width characters, half-spaces, or other invalid content.
